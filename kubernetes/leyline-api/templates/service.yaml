apiVersion: v1
kind: Service
metadata:
  name: {{ include "leyline-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "leyline-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leyline-api.selectorLabels" . | nindent 4 }}
